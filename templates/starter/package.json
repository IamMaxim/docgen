{
  "name": "{{projectName}}",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "predev": "docgen link-graph --config ./docs-site.config.json --out ./src/lib/link-graph.json",
    "dev": "vite dev",
    "prebuild": "docgen link-graph --config ./docs-site.config.json --out ./src/lib/link-graph.json",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "format": "prettier --write .",
    "lint": "prettier --check ."
  },
  "dependencies": {
    "@iammaxim/docgen": "^0.1.5",
    "katex": "^0.16.27"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.10",
    "@sveltejs/kit": "^2.48.5",
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "@types/node": "^20",
    "mdsvex": "^0.12.6",
    "prettier": "^3.6.2",
    "prettier-plugin-svelte": "^3.4.0",
    "svelte": "^5.43.8",
    "svelte-check": "^4.3.4",
    "typescript": "^5.9.3",
    "vite": "^7.2.2"
  }
}
